# LFS disabled for site assets (GitHub Pages serves LFS pointers as text unless using a custom workflow).
# If you need to store very large raw files, put them in a separate folder and re-enable selectively.
# *.MP4 filter=lfs diff=lfs merge=lfs -text
# *.MOV filter=lfs diff=lfs merge=lfs -text
